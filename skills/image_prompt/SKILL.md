# Skill: image_prompt

## 何时启用
当用户想要生成图片，或希望优化/扩写图像提示词时。

## 执行策略
1. 如果用户给的描述很短（少于 30 字），先扩写为包含以下要素的完整 prompt：
   - 主体（人物/场景/物品）
   - 构图与镜头（特写/中景/广角等）
   - 光线与色调
   - 风格（写实/动漫/油画等）
2. 中文 prompt 即可，不必翻译成英文
3. 调用 `generate_image(prompt=..., size="1024x1024")` 生成
4. 生成后告知用户文件名，并询问是否需要在不同尺寸/风格上重新生成
